# Layer KeyMap Editor

`index.html` can be opened directly in a browser. To use a local preview server:

```powershell
node server.js
```

Then open `http://127.0.0.1:4173`.

The editor imports legacy single-layer KeyMaps and layered KeyMaps. It exports standard JSON without uploading a configuration anywhere.
